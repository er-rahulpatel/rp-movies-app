import IndexContainer from "../containers/IndexContainer";

const IndexScreen = ({ navigation }) => {
    return (
        <IndexContainer navigation={navigation} />
    );
}
export default IndexScreen;